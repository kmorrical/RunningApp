(function() {
    'use strict';

    angular
        .module('app', []);
})();

/* This is called an immediately invoked function expression, an "iffy" The only 
reason we do this is so we can use strict*/